# TODO: Finish this grammar once the grammar for expressions is complete.
# IDEA: Type check the possible return values of the conditional expression.
#       If they are incompatible, then mark the macro as inconvertible.

grammar: |-

triads:
  - un-preprocessed: |-

    preprocessed: |-

    converted: |-
